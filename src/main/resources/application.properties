spring.application.name=db_khqr_merchant_uat
server.port=${PORT:1334}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/db_khqr_merchant_uat}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Da1219ri}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

logging.file.name=logs

app.jwt.secret=${APP_JWT_SECRET:StudentInfoJwtSecretKeyStudentInfoJwtSecretKey2026}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:86400000}
